<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="https://www.iciformation.fr/core/template/img/logo_iciformation.svg" alt="Logo" width="260px" class="d-inline-block align-text-center">
            Test Technique
        </a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Créer Un Utilisateur</a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="#">Formations</a>
            </li>
        </ul>
        <div class="nav-item dropdown ml-auto">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Mon Compte
            </a>
            <ul class="dropdown-menu">
                {% if is_granted('ROLE_USER') %}
                    <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>