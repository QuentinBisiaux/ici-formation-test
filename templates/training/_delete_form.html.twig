<form method="post" action="{{ path('app_training_delete', {'id': training.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer la formation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
